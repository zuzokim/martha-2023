# MARTHA - 2023 / client

### real-time communication web client

- to install dependencies

        ```
        npm install

        ```

- to run the app (default port is: `5173`)

        ```
        npm run dev

        ```
