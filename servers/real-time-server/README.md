 # MARTHA - 2023 / real-time-server

  - to install dependencies

    ```
    npm install

    ```

  - to start the server in dev & watch mode (default port is: `8000`)

    ```
    npm run dev

    ```